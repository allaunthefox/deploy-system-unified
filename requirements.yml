# =============================================================================
# Audit Event Identifier: DSU-PLY-100020
# Last Updated: 2026-02-28
# =============================================================================
---
# Ansible Collections Requirements
# Defines the collections required for the Deploy-System-Unified project
# Note: Run 'ansible-galaxy collection install -r requirements.yml --force' to update
#
# VERSION PINNING POLICY:
# - Use >=X.Y.Z,<X+1.0.0 for minor version updates (safe)
# - Use >=X.Y.Z,<Y.0.0 for major version pinning (breaking changes possible)
# - Update versions quarterly after testing in staging

collections:
  # Core Ansible collections (pinned for stability)
  - name: ansible.posix
    version: ">=1.5.4,<2.0.0"
  - name: ansible.utils
    version: ">=6.0.1,<7.0.0"
  - name: community.general
    version: ">=12.3.0,<13.0.0"
  - name: community.crypto
    version: ">=2.17.0,<3.0.0"
  - name: community.docker
    version: ">=5.0.5,<6.0.0"

  # Container and cloud collections
  - name: containers.podman
    version: ">=1.18.1,<2.0.0"

  # SOPS encryption collection
  - name: community.sops
    version: ">=1.6.0,<2.0.0"

  # Additional required collections
  - name: community.libvirt
    version: ">=1.3.0,<2.0.0"
  - name: community.library_inventory_filtering_v1
    version: ">=1.1.5,<2.0.0"
