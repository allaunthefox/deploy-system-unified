# =============================================================================
# Audit Event Identifier: DSU-PLY-100001
# Playbook Type: Entry Point (Canonical)
# Description: Main entry point - proxies to production_deploy.yml
# Last Updated: 2026-02-28
# Version: 1.0
# =============================================================================
---
# WARNING: This playbook is part of the solution stack.
# Use production_deploy.yml as the primary deployment entrypoint.

# Unified Deployment Playbook
# Cleaned up to simply proxy to the canonical production_deploy.yml.
# Custom adjustments should now be handled via group_vars or Ansible tags.

- ansible.builtin.import_playbook: production_deploy.yml
