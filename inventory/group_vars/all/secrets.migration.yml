---
# MIGRATION SECRETS - AUDIT REDACTED
# DANGER: Previous version contained plaintext credentials.
# These values MUST be provided via SOPS or Vault at runtime.

authentik_secret_key: "{{ lookup('env', 'AUTHENTIK_SECRET_KEY') | default('CHANGE_ME_IN_SOPS', true) }}"
authentik_pg_password: "{{ lookup('env', 'AUTHENTIK_PG_PASSWORD') | default('CHANGE_ME_IN_SOPS', true) }}"
wikijs_pg_password: "{{ lookup('env', 'WIKIJS_PG_PASSWORD') | default('CHANGE_ME_IN_SOPS', true) }}"
vaultwarden_admin_token: "{{ lookup('env', 'VAULTWARDEN_ADMIN_TOKEN') | default('CHANGE_ME_IN_SOPS', true) }}"
database_password: "{{ lookup('env', 'DATABASE_PASSWORD') | default('CHANGE_ME_IN_SOPS', true) }}"
