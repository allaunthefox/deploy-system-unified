# =============================================================================
# Audit Event Identifier: DSU-HLM-300056
# Last Updated: 2026-02-28
# =============================================================================
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "ops-stack.fullname" . }}-homarr-configs
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.homarr.persistence.configs.size }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "ops-stack.fullname" . }}-homarr-icons
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.homarr.persistence.icons.size }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "ops-stack.fullname" . }}-homarr-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.homarr.persistence.data.size }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "ops-stack.fullname" . }}-vaultwarden-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.vaultwarden.persistence.data.size }}
