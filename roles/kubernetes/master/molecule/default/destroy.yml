---
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Remove Kubernetes master test container
      containers.podman.podman_container:
        name: k8s-master-molecule
        state: absent
        force_delete: true
