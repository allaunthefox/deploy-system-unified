---
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Remove Kubernetes node test container
      containers.podman.podman_container:
        name: k8s-node-molecule
        state: absent
        force_delete: true
