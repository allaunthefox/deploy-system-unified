# =============================================================================
# Audit Event Identifier: DSU-PLY-100599
# Last Updated: 2026-02-28
# =============================================================================
---
argument_specs:
  main:
    short_description: K8s node role
    description:
      - Installs and configures Kubernetes worker nodes using K3s Agent
      - Joins worker nodes to an existing Kubernetes cluster
      - Aligns with ISO 27001 §8.26 for orchestration cluster security
    options:
      kubernetes_server_url:
        type: str
        description: URL of the Kubernetes master server
      kubernetes_node_token:
        type: str
        description: Token used to join the cluster
