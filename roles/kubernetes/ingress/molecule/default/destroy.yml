# =============================================================================
# Audit Event Identifier: DSU-TST-1000517
# Last Updated: 2026-02-28
# =============================================================================
---
# CIS Kubernetes Benchmark | ISO 27001 §14.2
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Destroy ingress test containers
      containers.podman.podman_container:
        name: "{{ item }}"
        state: absent
      loop:
        - ingress-ubuntu-2204
        - ingress-debian-12
