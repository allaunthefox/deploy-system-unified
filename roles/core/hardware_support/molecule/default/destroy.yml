# =============================================================================
# Audit Event Identifier: DSU-TST-1000402
# Last Updated: 2026-02-28
# =============================================================================
---
# NIST SP 800-53 CM-5 | ISO 27001 §12.1
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Remove hardware_support test containers
      containers.podman.podman_container:
        name: "{{ item }}"
        state: absent
        force_delete: true
      loop:
        - hardware-support-ubuntu-2204
        - hardware-support-debian-12
        - hardware-support-alpine-319
        - hardware-support-rockylinux-9
