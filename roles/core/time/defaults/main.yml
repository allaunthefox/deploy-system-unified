# =============================================================================
# Audit Event Identifier: DSU-PLY-100488
# Last Updated: 2026-02-28
# =============================================================================
---
# NIST SP 800-53 CM-5 | ISO 27001 §12.1
# Defaults for core/time role
core_time_service_mapping:
  archlinux:
    service: chronyd
    package: chrony
    conf_dir: "/etc"
  debian:
    service: chrony
    package: chrony
    conf_dir: "/etc/chrony"
  ubuntu:
    service: chrony
    package: chrony
    conf_dir: "/etc/chrony"
  fedora:
    service: chronyd
    package: chrony
    conf_dir: "/etc"
  alpine:
    service: chronyd
    package: chrony
    conf_dir: "/etc/chrony"
