# =============================================================================
# Audit Event Identifier: DSU-ARG-100483
# Last Updated: 2026-03-01
# =============================================================================
---
argument_specs:
  main:
    short_description: Core systemd configuration and hardening
    description:
      - This role configures and hardens systemd components, specifically focusing on journald logging and resolved DNS services.
    options:
      systemd_configure_journald:
        type: bool
        default: true
        description: Whether to apply the project's journald hardening configuration.
      systemd_configure_resolved:
        type: bool
        default: true
        description: Whether to apply the project's systemd-resolved configuration.
      systemd_persistent_journal:
        type: bool
        default: true
        description: Whether to ensure journal logs persist across reboots.
