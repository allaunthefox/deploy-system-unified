# =============================================================================
# Audit Event Identifier: DSU-ARG-100443
# Last Updated: 2026-03-01
# =============================================================================
---
argument_specs:
  main:
    short_description: System and object identity management
    description:
      - This role manages the system hostname and ensures a unique UUID is assigned to the host for forensic tracking.
    options:
      identity_set_hostname:
        type: bool
        default: true
        description: Whether to manage and set the system hostname.
      identity_domain:
        type: str
        default: "local"
        description: The internal domain name to append to the system identity.
