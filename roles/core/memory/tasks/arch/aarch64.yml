# =============================================================================
# Audit Event Identifier: DSU-PLY-100460
# Last Updated: 2026-02-28
# =============================================================================
---
# NIST SP 800-53 CM-5 | ISO 27001 §12.1
# aarch64 specific memory tasks

- name: Detect Shared Video Memory (UMA) - aarch64
  tags: [core, memory, remediate]
  ansible.builtin.set_fact:
    uma_detection:
      stdout: "false" # Default for many ARM SBCs unless we add specific checks
  when: core_memory_shared_vram == 'auto'
