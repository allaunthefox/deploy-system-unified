# =============================================================================
# Audit Event Identifier: DSU-ARG-100405
# Last Updated: 2026-03-01
# =============================================================================
---
argument_specs:
  main:
    short_description: System initialization and base configuration
    description:
      - This role initializes the target host by installing base packages, creating standard directories, and setting virtualization facts.
    options:
      core_install_base_packages:
        type: bool
        default: true
        description: Whether to install the project's baseline package set.
      system_base_packages:
        type: dict
        description: Dictionary mapping OS families to lists of required packages.
      system_standard_directories:
        type: list
        elements: str
        description: List of absolute paths to ensure exist on the target system.
