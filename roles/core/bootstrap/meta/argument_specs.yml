---
argument_specs:
  main:
    short_description: Core system bootstrap role
    description:
      - Initializes the system with base packages and directories
      - Detects virtualization environment and determines ontology-driven virt_type
      - Generates a unique deployment ID for forensic traceability
    options:
      core_install_base_packages:
        type: bool
        description: Whether to install the base package set
        default: true
      system_base_packages:
        type: dict
        description: Mapping of OS families to lists of essential packages
      system_standard_directories:
        type: list
        elements: str
        description: List of standard directories to ensure exist
