# =============================================================================
# Audit Event Identifier: DSU-PLY-100412
# Last Updated: 2026-02-28
# =============================================================================
---
# NIST SP 800-53 CM-5 | ISO 27001 §12.1
# Defaults for core/entropy role
entropy_service_mapping:
  archlinux:
    entropy_service: haveged
    entropy_package: haveged
    rng_service: rngd
    rng_package: rng-tools
  debian:
    entropy_service: haveged
    entropy_package: haveged
    rng_service: rng-tools
    rng_package: rng-tools5
  ubuntu:
    entropy_service: haveged
    entropy_package: haveged
    rng_service: rng-tools
    rng_package: rng-tools5
  fedora:
    entropy_service: haveged
    entropy_package: haveged
    rng_service: rngd
    rng_package: rng-tools
  alpine:
    entropy_service: haveged
    entropy_package: haveged
    rng_service: rngd
    rng_package: rng-tools
