# =============================================================================
# Audit Event Identifier: DSU-TST-1000387
# Last Updated: 2026-02-28
# =============================================================================
---
# NIST SP 800-53 CM-5 | ISO 27001 §12.1
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Remove entropy test containers
      containers.podman.podman_container:
        name: "{{ item }}"
        state: absent
        force_delete: true
      loop:
        - entropy-ubuntu-2204
        - entropy-debian-12
        - entropy-alpine-319
        - entropy-rockylinux-9
