---
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Remove updates test containers
      containers.podman.podman_container:
        name: "{{ item }}"
        state: absent
        force_delete: true
      loop:
        - updates-ubuntu-2204
        - updates-debian-12
        - updates-alpine-319
        - updates-rockylinux-9
