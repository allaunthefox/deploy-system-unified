# =============================================================================
# Audit Event Identifier: DSU-TST-1000375
# Last Updated: 2026-02-28
# =============================================================================
---
# NIST SP 800-53 | ISO 27001 §14.2
dependency:
  name: galaxy
driver:
  name: podman
platforms:
  - name: kyverno-test
    image: docker.io/rancher/k3s:latest
    command: /bin/sh -c "mount --make-rshared / && /bin/k3s server --disable traefik"
    privileged: true
    tmpfs:
      - /run
      - /var/run
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
      - /var/lib/containerd
provisioner:
  name: ansible
verifier:
  name: ansible
