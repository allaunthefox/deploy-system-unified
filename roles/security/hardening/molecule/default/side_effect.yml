---
- name: Side Effect
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Side effect placeholder
      ansible.builtin.debug:
        msg: "No side effects for security/hardening Molecule scenario."
