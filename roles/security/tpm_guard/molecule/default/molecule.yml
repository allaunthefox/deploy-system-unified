---
dependency:
  name: galaxy
driver:
  name: delegated
platforms:
  - name: instance
provisioner:
  name: ansible
  config_options:
    defaults:
      host_key_checking: true
verifier:
  name: ansible
scenario:
  test_sequence:
    - converge
    - verify
