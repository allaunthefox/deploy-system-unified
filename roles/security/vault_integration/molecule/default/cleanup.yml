---
- name: Cleanup
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: Cleanup vault files
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - /etc/vault
        - /var/lib/vault
        - /var/log/vault
      failed_when: false
