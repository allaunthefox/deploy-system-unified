# =============================================================================
# Audit Event Identifier: DSU-TST-1000367
# Last Updated: 2026-02-28
# =============================================================================
---
# NIST SP 800-53 | ISO 27001 §14.2
- name: Cleanup
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: Cleanup vault files
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - /etc/vault
        - /var/lib/vault
        - /var/log/vault
      failed_when: false
