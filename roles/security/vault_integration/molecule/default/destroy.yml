---
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Destroy vault test containers
      containers.podman.podman_container:
        name: "{{ item }}"
        state: absent
      loop:
        - vault-ubuntu-2204
        - vault-debian-12
