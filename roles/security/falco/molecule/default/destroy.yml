# =============================================================================
# Audit Event Identifier: DSU-TST-1000249
# Last Updated: 2026-02-28
# =============================================================================
---
# NIST SP 800-53 | ISO 27001 §14.2
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Destroy falco test containers
      containers.podman.podman_container:
        name: "{{ item }}"
        state: absent
      loop:
        - falco-ubuntu-2204
        - falco-debian-12
