---
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Destroy falco test containers
      containers.podman.podman_container:
        name: "{{ item }}"
        state: absent
      loop:
        - falco-ubuntu-2204
        - falco-debian-12
