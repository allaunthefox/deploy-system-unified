---
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Remove access test containers
      containers.podman.podman_container:
        name: "{{ item }}"
        state: absent
        force_delete: true
      loop:
        - access-ubuntu-2204
        - access-debian-12
        - access-alpine-319
        - access-rockylinux-9
