# =============================================================================
# Audit Event Identifier: DSU-PLY-100804
# Last Updated: 2026-03-01
# =============================================================================
---
# Handlers for security/headscale
# ISO 27001 §12.1

- name: "ISO 27001 §12.1 | 600013 | reload_systemd"
  ansible.builtin.systemd:
    daemon_reload: true
  become: true
  listen: "reload systemd"

- name: "ISO 27001 §12.1 | 800110 | restart_headscale"
  ansible.builtin.systemd:
    name: headscale
    state: restarted
  become: true
  listen: "restart headscale"
