# =============================================================================
# Audit Event Identifier: DSU-PLY-100335
# Last Updated: 2026-03-01
# =============================================================================
---
# Defaults for security/aws_secrets role
aws_secrets_enable: true
aws_region: "us-east-1"
aws_auth_method: "instance_profile"  # Choices: instance_profile, credentials, assume_role

# Secret Mappings
# - { name: "prod/db/pass", path: "/etc/secrets/db_pass", format: "plain" }
aws_secrets_items: []

# Configuration
aws_secrets_config_dir: "/etc/aws-secrets"
aws_secrets_owner: "root"
aws_secrets_group: "root"
