# =============================================================================
# Audit Event Identifier: DSU-TST-1003053
# Test Type: Molecule Verify Phase
# Last Updated: 2026-02-28
# Version: 1.0
# =============================================================================
---
# NIST SP 800-53 | ISO 27001 §14.2
- name: Verify
  hosts: all
  tasks:
    - name: Check IMA configuration file existence
      ansible.builtin.stat:
        path: /etc/ima/ima-policy
      register: ima_policy
    - name: Assert policy was deployed
      ansible.builtin.assert:
        that:
          - ima_policy.stat.exists
