# =============================================================================
# Audit Event Identifier: DSU-TST-1000476
# Last Updated: 2026-02-28
# =============================================================================
---
# ISO 27001 §11.1 | Physical Security
dependency:
  name: galaxy
driver:
  name: default
  options:
    managed: false
    ansible_connection_options:
      ansible_connection: containers.podman.podman
platforms:
  - name: firmware-ubuntu-2204
    image: docker.io/geerlingguy/docker-ubuntu2204-ansible:latest
  - name: firmware-debian-12
    image: docker.io/geerlingguy/docker-debian12-ansible:latest
  - name: firmware-rockylinux-9
    image: docker.io/geerlingguy/docker-rockylinux9-ansible:latest
prerun: false
provisioner:
  name: ansible
  config_options:
    defaults:
      deprecation_warnings: false
  inventory:
    host_vars:
      firmware-ubuntu-2204:
        ansible_connection: containers.podman.podman
        ansible_host: firmware-ubuntu-2204
      firmware-debian-12:
        ansible_connection: containers.podman.podman
        ansible_host: firmware-debian-12
      firmware-rockylinux-9:
        ansible_connection: containers.podman.podman
        ansible_host: firmware-rockylinux-9
  env:
    ANSIBLE_ROLES_PATH: ../../../../
  playbooks:
    create: create.yml
    destroy: destroy.yml
    converge: converge.yml
    prepare: prepare.yml
    cleanup: cleanup.yml
    side_effect: side_effect.yml
    verify: verify.yml
verifier:
  name: ansible
scenario:
  test_sequence:
    - create
    - prepare
    - converge
    - idempotence
    - verify
    - cleanup
    - destroy
