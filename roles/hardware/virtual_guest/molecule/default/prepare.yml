---
- name: Prepare
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Prepare placeholder
      ansible.builtin.debug:
        msg: "No prepare steps required for hardware/virtual_guest."
