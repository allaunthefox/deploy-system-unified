---
# Handlers for hardware/storage_tuning
- name: reload_udev
  ansible.builtin.command: udevadm control --reload-rules
  become: true
  listen: "reload udev"
  failed_when: false
  changed_when: false
