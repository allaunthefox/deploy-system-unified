# =============================================================================
# Audit Event Identifier: DSU-TST-1000484
# Last Updated: 2026-02-28
# =============================================================================
---
# ISO 27001 §11.1 | Physical Security
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Remove gpu test containers
      containers.podman.podman_container:
        name: "{{ item }}"
        state: absent
        force_delete: true
      loop:
        - gpu-ubuntu-2204
        - gpu-debian-12
        - gpu-alpine-319
        - gpu-rockylinux-9
