# =============================================================================
# Audit Event Identifier: DSU-PLY-100526
# Last Updated: 2026-02-28
# =============================================================================
---
galaxy_info:
  role_name: gpu
  author: deploysystemunified
  description: Universal GPU driver orchestration and compute stack management (NVIDIA, AMD, Intel)
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions: [all]
    - name: Debian
      versions: [all]
    - name: Fedora
      versions: [all]
    - name: Archlinux
      versions: [all]
  galaxy_tags:
    - hardware
    - gpu
    - nvidia
    - amd
    - intel
    - rocm
    - cuda
    - oneapi
    - compute
    - drivers
    - virtualization
    - slicing
    - multiarch
    - iso27001

collections:
  - ansible.builtin
  - community.general

dependencies: []
