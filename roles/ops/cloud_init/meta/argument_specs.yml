# =============================================================================
# Audit Event Identifier: DSU-PLY-100181
# Last Updated: 2026-02-28
# =============================================================================
---
argument_specs:
  main:
    short_description: cloud_init role for cloud-init configuration
    description:
      - Configures cloud-init for instance initialization
      - Manages cloud-config, user-data, and metadata
      - Aligns with ISO 9001 requirements for process synchronization
    options:
      cloud_init_enable:
        type: bool
        required: false
        default: true
        description:
          - Enable or disable cloud_init role execution
      cloud_init_config:
        type: dict
        required: false
        default: {}
        description:
          - Cloud-init configuration dictionary
