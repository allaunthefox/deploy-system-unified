---
# Goss tests for ops/guest_management role
# This role is a placeholder for guest management operations
#
# Compliance: CIS 4.10.x, STIG V-230760, NIST CM-6, ISO 27001 §8.15/§8.16
#
# These tests verify:
# - The role can execute without errors
# - Basic system requirements are met
#
# Note: This is currently a placeholder role with no specific functionality

# Command checks - basic system commands
command:
  # Verify basic system is operational
  echo "guest_management_role_active":
    exit-status: 0
    stderr: []
    timeout: 5000
    stdout:
      - "guest_management_role_active"

  # Verify hostname resolution works
  hostname -f:
    exit-status: 0
    stderr: []
    timeout: 5000

# File checks - basic system files
file:
  /etc/hostname:
    exists: true
    mode: "0644"
    owner: root
    group: root

  /etc/hosts:
    exists: true
    mode: "0644"
    owner: root
    group: root

# Process check - verify basic system is running
process:
  init:
    running: true
