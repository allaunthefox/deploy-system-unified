# =============================================================================
# Audit Event Identifier: DSU-TST-1000158
# Last Updated: 2026-02-28
# =============================================================================
---
# ISO 27001 §12.4 | System Monitoring
- name: Verify
  hosts: all
  tasks:
    - name: Assert connection info facts are set
      ansible.builtin.assert:
        that:
          - ssh_connection_info is defined
          - ssh_connection_info.hostname is defined
          - ssh_connection_info.port is defined
        fail_msg: "Connection info facts are not properly set"
