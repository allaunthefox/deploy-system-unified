# =============================================================================
# Audit Event Identifier: DSU-TST-1000612
# Last Updated: 2026-02-28
# =============================================================================
---
# ISO 27001 §12.1 | Operational Procedures
- name: Create
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Create identity test container
      containers.podman.podman_container:
        name: k8s-node-molecule
        image: docker.io/geerlingguy/docker-ubuntu2204-ansible:latest
        command: /lib/systemd/systemd
        privileged: true
        volumes:
          - /sys/fs/cgroup:/sys/fs/cgroup:rw
        tmpfs:
          /run: "rw"
          /tmp: "rw"
        state: started
