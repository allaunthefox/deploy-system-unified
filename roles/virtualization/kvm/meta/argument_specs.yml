# =============================================================================
# Audit Event Identifier: DSU-PLY-100745
# Last Updated: 2026-02-28
# =============================================================================
---
argument_specs:
  main:
    short_description: Virtualization KVM role
    description:
      - Installs KVM, QEMU, and libvirt stack
      - Configures restrictive security settings for the hypervisor
      - Ensures necessary kernel modules are loaded for virtualization
    options:
      virtualization_kvm_enabled:
        type: bool
        description: Enable or disable the KVM role
        default: true
