---
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Remove KVM test container
      containers.podman.podman_container:
        name: kvm-molecule
        state: absent
        force_delete: true
