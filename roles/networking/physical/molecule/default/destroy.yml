# =============================================================================
# Audit Event Identifier: DSU-TST-1000568
# Last Updated: 2026-02-28
# =============================================================================
---
# RFC Compliance | ISO 27001 §13.1
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Remove physical test containers
      containers.podman.podman_container:
        name: "{{ item }}"
        state: absent
        force_delete: true
      loop:
        - physical-ubuntu-2204
        - physical-debian-12
        - physical-alpine-319
        - physical-rockylinux-9
