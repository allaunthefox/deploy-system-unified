# =============================================================================
# Audit Event Identifier: DSU-PLY-100656
# Last Updated: 2026-02-28
# =============================================================================
---
argument_specs:
  main:
    short_description: Virtual networking role
    description:
      - Manages virtual network overlays and segmentation
      - Implements VLAN tagging and VXLAN encapsulation
      - Aligns with ISO 27001 §9.4 requirements for network isolation
    options:
      networking_virtual_enabled:
        type: bool
        description: Enable virtual networking management
        default: true
