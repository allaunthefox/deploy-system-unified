# =============================================================================
# Audit Event Identifier: DSU-TST-1003057
# Test Type: Molecule Verify Phase
# Last Updated: 2026-02-28
# Version: 1.0
# =============================================================================
---
# RFC Compliance | ISO 27001 §13.1
- name: Verify
  hosts: all
  tasks:
    - name: Verify istioctl installed
      ansible.builtin.command: /usr/local/bin/istioctl version --remote=false
      register: result
      failed_when: result.rc != 0
