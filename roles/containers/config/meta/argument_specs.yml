# =============================================================================
# Audit Event Identifier: DSU-PLY-100065
# Last Updated: 2026-02-28
# =============================================================================
---
# CIS Docker Benchmark | Pod Security Standards
argument_specs:
  main:
    short_description: Container configuration role
    description:
      - Manages global container storage settings in storage.conf
      - Configures user lingering for rootless Podman services
      - Prepares standard container storage directories
    options:
      container_linger_users:
        type: list
        elements: str
        description: List of users to enable lingering for
        default: []
