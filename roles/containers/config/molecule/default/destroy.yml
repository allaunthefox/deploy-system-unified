# =============================================================================
# Audit Event Identifier: DSU-TST-1000067
# Last Updated: 2026-02-28
# =============================================================================
---
# CIS Docker Benchmark | Pod Security Standards
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Remove identity test container
      containers.podman.podman_container:
        name: config-molecule
        state: absent
        force_delete: true
