# =============================================================================
# Audit Event Identifier: DSU-TST-1000028
# Last Updated: 2026-02-28
# =============================================================================
---
# CIS Docker Benchmark | Pod Security Standards
dependency:
  name: galaxy
driver:
  name: default
  options:
    managed: false
    ansible_connection_options:
      ansible_connection: containers.podman.podman
platforms:
  - name: anubis-ubuntu-2204
    image: docker.io/geerlingguy/docker-ubuntu2204-ansible:latest
  - name: anubis-debian-12
    image: docker.io/geerlingguy/docker-debian12-ansible:latest
  - name: anubis-alpine-319
    image: docker.io/geerlingguy/docker-alpine319-ansible:latest
  - name: anubis-rockylinux-9
    image: docker.io/geerlingguy/docker-rockylinux9-ansible:latest
prerun: false
provisioner:
  name: ansible
  config_options:
    defaults:
      deprecation_warnings: false
  inventory:
    host_vars:
      anubis-ubuntu-2204:
        ansible_connection: containers.podman.podman
        ansible_host: anubis-ubuntu-2204
      anubis-debian-12:
        ansible_connection: containers.podman.podman
        ansible_host: anubis-debian-12
      anubis-alpine-319:
        ansible_connection: containers.podman.podman
        ansible_host: anubis-alpine-319
      anubis-rockylinux-9:
        ansible_connection: containers.podman.podman
        ansible_host: anubis-rockylinux-9
  env:
    ANSIBLE_ROLES_PATH: ../../../../
  playbooks:
    create: create.yml
    destroy: destroy.yml
    converge: converge.yml
    prepare: prepare.yml
    cleanup: cleanup.yml
    side_effect: side_effect.yml
    verify: verify.yml
verifier:
  name: ansible
scenario:
  test_sequence:
    - create
    - prepare
    - converge
    - idempotence
    - verify
    - cleanup
    - destroy
