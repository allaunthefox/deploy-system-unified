---
galaxy_info:
  role_name: caddy
  author: deploy-system-unified
  description: "WI-CONT | Caddy reverse proxy with automatic HTTPS and CrowdSec integration"
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions: [all]
    - name: Debian
      versions: [all]
    - name: Fedora
      versions: [all]
    - name: Archlinux
      versions: [all]
    - name: Alpine
      versions: [all]
  galaxy_tags: [containers, caddy]
# 'assumes_permissive' removed: not valid RoleMetadata attribute for Ansible;
# role behavior for permissive profiles is documented in README or role defaults.
# Caddy may rely on CrowdSec keys and host-networked services in permissive profiles.

dependencies: []
