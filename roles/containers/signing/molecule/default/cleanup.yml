# =============================================================================
# Audit Event Identifier: DSU-TST-1000134
# Last Updated: 2026-02-28
# =============================================================================
---
# CIS Docker Benchmark | Pod Security Standards
- name: Cleanup
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: Cleanup cosign files
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - /etc/cosign
        - /var/log/cosign
      failed_when: false
