---
- name: Side Effect
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: Side effect placeholder
      ansible.builtin.debug:
        msg: "No side effects for containers/signing."
