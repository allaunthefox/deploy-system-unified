# =============================================================================
# Audit Event Identifier: DSU-TST-1000137
# Last Updated: 2026-02-28
# =============================================================================
---
# CIS Docker Benchmark | Pod Security Standards
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Destroy signing test containers
      containers.podman.podman_container:
        name: "{{ item }}"
        state: absent
      loop:
        - signing-ubuntu-2204
        - signing-debian-12
