{# =============================================================================
# Audit Event Identifier: DSU-TPL-900007
# Template Type: monitoring Configuration
# Last Updated: 2026-02-28
# Version: 1.0
# ============================================================================= #}
# Prometheus Configuration (ISO 27001 §12.4 | Audit Code 840000)
# Forensic UUID: {{ object_uuid | default('unassigned') }}
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'node_exporter'
    static_configs:
      # Accessing the host IP from within the container network.
      # Usually host.containers.internal or the gateway IP.
      # For now, we assume the node_exporter binds to host interface.
      - targets: ['{{ ansible_default_ipv4.address }}:9100']
