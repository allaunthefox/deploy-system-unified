---
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Remove identity test container
      containers.podman.podman_container:
        name: lxc-molecule
        state: absent
        force_delete: true
