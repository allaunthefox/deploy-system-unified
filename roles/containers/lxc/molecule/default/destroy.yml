# =============================================================================
# Audit Event Identifier: DSU-TST-1000076
# Last Updated: 2026-02-28
# =============================================================================
---
# CIS Docker Benchmark | Pod Security Standards
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Remove identity test container
      containers.podman.podman_container:
        name: lxc-molecule
        state: absent
        force_delete: true
