# =============================================================================
# Audit Event Identifier: DSU-ARG-100250
# Last Updated: 2026-03-01
# =============================================================================
---
argument_specs:
  main:
    short_description: Shared backup security and integrity settings
    description:
      - This role provides common security settings used by all backup implementations (Restic, Rclone, Timeshift) to ensure cryptographic integrity.
    options:
      backup_security_enabled:
        type: bool
        default: true
        description: Whether to enforce hash verification and security checks for backups.
      backup_security_dir:
        type: str
        default: "/var/backups/security"
        description: Directory to store backup integrity manifests and hash lists.
