# =============================================================================
# Audit Event Identifier: DSU-TST-1000645
# Last Updated: 2026-02-28
# =============================================================================
---
# NIST Data-at-Rest | ISO 27001 §8.2
- hosts: instance
  connection: local
  gather_facts: false
  vars:
    restic_enable: true
    restic_fail_secure: true
    restic_password: "supersecret"
  tasks:
    - name: Run restic verify_secrets directly
      ansible.builtin.include_tasks: "../../tasks/verify_secrets.yml"
