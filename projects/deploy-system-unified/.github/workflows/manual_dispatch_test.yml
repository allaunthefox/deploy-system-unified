name: Manual Dispatch Test

on:
  workflow_dispatch:

jobs:
  test-dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Echo test
        run: |
          echo "Manual dispatch workflow executed"
          echo "HEAD: $GITHUB_SHA"
