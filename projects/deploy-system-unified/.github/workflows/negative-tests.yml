---
name: Negative Testing for Permissive Roles

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'roles/containers/**'
      - '.github/workflows/negative-tests.yml'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'roles/containers/**'
      - '.github/workflows/negative-tests.yml'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  negative-tests:
    name: Negative Testing for Permissive Roles
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        role:
          - containers/ops
          - containers/caddy
          - containers/authentik
          - containers/media
        test_case:
          - missing_file
          - wrong_permissions
          - placeholder_values
          - wrong_owner
        include:
          # Additional test cases for specific roles
          - role: containers/caddy
            test_case: missing_ssl_dir
          - role: containers/caddy
            test_case: wrong_ssl_permissions
          - role: containers/authentik
            test_case: missing_redis_file
          - role: containers/authentik
            test_case: empty_file
          - role: containers/media
            test_case: missing_jellyfin_file
          - role: containers/media
            test_case: missing_radarr_file
          - role: containers/media
            test_case: empty_media_file
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
<<<<<<< HEAD
      - name: Install Podman via apt
        run: |
          sudo apt-get update
          sudo apt-get install -y podman
          podman --version
=======
      - name: Install Podman via apt
        run: |
          sudo apt-get update
          sudo apt-get install -y podman
          podman --version


      - name: Check Podman availability
        run: |
          podman info || true

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements*.txt','**/pyproject.toml') }}

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies (ansible, molecule + podman plugin)
        run: |
          python -m pip install --upgrade pip
          pip install "ansible-core>=2.15,<2.16" ansible-lint yamllint "molecule[podman]" molecule-podman molecule-verifier-none testinfra

      - name: Cache Ansible Galaxy collections
        uses: actions/cache@v4
        with:
          path: |
            ~/.ansible/collections
          key: ${{ runner.os }}-ansible-collections-${{ hashFiles('**/collections.yml') }}

      - name: Install Ansible collections
        run: |
          ansible-galaxy collection install ansible.posix community.general containers.podman

      - name: Run negative tests
        run: |
          cd roles/${{ matrix.role }}
          echo "Running negative test: ${{ matrix.test_case }} for role ${{ matrix.role }}"
          test_case=${{ matrix.test_case }} molecule test --scenario-name negative
        env:
          test_case: ${{ matrix.test_case }}
          
      - name: Set artifact name
        if: always()
        run: |
          ART="${{ matrix.role }}"
          ART=${ART//\//-}
          echo "ARTIFACT_NAME=$ART" >> $GITHUB_ENV

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: ${{ env.ARTIFACT_NAME }}-${{ matrix.test_case }}
          path: |
            ${{ matrix.role }}/molecule
            ${{ matrix.role }}/.molecule
          if-no-files-found: warn

  validate-workflow:
    name: Validate GitHub Actions Workflow
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Validate workflow syntax
        run: |
          echo "Validating .github/workflows/negative-tests.yml"
          python -c "import yaml; yaml.safe_load(open('.github/workflows/negative-tests.yml')); print('✅ Workflow syntax is valid')"
