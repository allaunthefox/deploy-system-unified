---
# Distribution-aware service and package mapping for Deploy-System-Unified

# Note: Keys are lower-case to match ansible_distribution | lower

preflight_distro_service_mapping:
  archlinux:
    time_sync_service: chronyd
    time_sync_package: chrony
    entropy_service: haveged
    entropy_package: haveged
    rng_service: rngd
    rng_package: rng-tools
  debian:
    time_sync_service: chrony
    time_sync_package: chrony
    entropy_service: haveged
    entropy_package: haveged
    rng_service: rngd
    rng_package: rng-tools
  ubuntu:
    time_sync_service: chrony
    time_sync_package: chrony
    entropy_service: haveged
    entropy_package: haveged
    rng_service: rngd
    rng_package: rng-tools
  fedora:
    time_sync_service: chronyd
    time_sync_package: chrony
    entropy_service: haveged
    entropy_package: haveged
    rng_service: rngd
    rng_package: rng-tools
  alpine:
    time_sync_service: chronyd
    time_sync_package: chrony
    entropy_service: haveged
    entropy_package: haveged
    rng_service: rngd
    rng_package: rng-tools


preflight_distro_package_mapping:
  archlinux:
    security_packages:
      - trivy
      - trufflehog
      - bubblewrap
      - firejail
      - endlessh
      - aide
      - chkrootkit
      - rkhunter
      - lynis
      - checkov
      - tfsec
      - terrascan
  debian:
    security_packages:
      - trivy
      - trufflehog-go
      - bubblewrap
      - firejail
      - endlessh
      - aide
      - chkrootkit
      - rkhunter
      - lynis
      - checkov
      - tfsec
      - terrascan
  ubuntu:
    security_packages:
      - trivy
      - trufflehog-go
      - bubblewrap
      - firejail
      - endlessh
      - aide
      - chkrootkit
      - rkhunter
      - lynis
      - checkov
      - tfsec
      - terrascan
  fedora:
    security_packages:
      - trivy
      - trufflehog
      - bubblewrap
      - firejail
      - endlessh
      - aide
      - chkrootkit
      - rkhunter
      - lynis
      - checkov
      - tfsec
      - terrascan
  alpine:
    security_packages:
      - curl
      - jq
      - bash
      - chrony
      - haveged
      - rng-tools
      - checkov
      - tfsec
      - terrascan
