---
# Defaults for core/entropy role
entropy_service_mapping:
  archlinux:
    entropy_service: haveged
    entropy_package: haveged
    rng_service: rngd
    rng_package: rng-tools
  debian:
    entropy_service: haveged
    entropy_package: haveged
    rng_service: rngd
    rng_package: rng-tools
  ubuntu:
    entropy_service: haveged
    entropy_package: haveged
    rng_service: rngd
    rng_package: rng-tools
  fedora:
    entropy_service: haveged
    entropy_package: haveged
    rng_service: rngd
    rng_package: rng-tools
  alpine:
    entropy_service: haveged
    entropy_package: haveged
    rng_service: rngd
    rng_package: rng-tools
