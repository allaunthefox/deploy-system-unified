---
# Defaults for ops/connection_info role
encryption_method: "plain" # sops or vault (Default changed to plain for dev/test without keys)
ssh_rsync_destination: ""
ssh_randomize_port: false
access_sshd_port: 22
