---
# Containers role - Main entry point that imports sub-roles
- name: Import containers runtime tasks
  ansible.builtin.import_tasks: runtime/tasks/main.yml
  tags: [containers, runtime]
- name: Import containers config tasks
  ansible.builtin.import_tasks: config/tasks/main.yml
  tags: [containers, config]
- name: Import containers caddy tasks
  ansible.builtin.import_tasks: caddy/tasks/main.yml
  tags: [containers, caddy]
- name: Import containers quadlets tasks
  ansible.builtin.import_tasks: quadlets/tasks/main.yml
  tags: [containers, quadlets]
