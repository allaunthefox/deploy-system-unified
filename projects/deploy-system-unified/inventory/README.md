This directory contains inventory files for local testing.

The file `examples/local.ini` is an example inventory for running the playbooks locally. Do not commit production inventory files here; keep real inventories out of the repository or encrypted.
