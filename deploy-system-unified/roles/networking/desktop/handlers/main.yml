---
# Handlers for networking/desktop

- name: restart networkmanager
  ansible.builtin.service:
    name: "{{ _net_service_name | default('NetworkManager') }}"
    state: restarted
  become: true

- name: restart_networkmanager
  ansible.builtin.service:
    name: "{{ _net_service_name | default('NetworkManager') }}"
    state: restarted
  become: true
