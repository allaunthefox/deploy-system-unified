---
dependency:
  name: galaxy
driver:
  name: default
platforms:
  - name: instance
provisioner:
  name: ansible
  inventory:
    hosts:
      instance:
        ansible_connection: local
  playbooks:
    converge: converge.yml
verifier:
  name: ansible
