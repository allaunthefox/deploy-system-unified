---
# Handlers for hardware/firmware
- name: restart_watchdog
  ansible.builtin.systemd:
    name: watchdog
    state: restarted
  become: true
  listen: "restart watchdog"
  when: not _hw_is_container | default(false)
