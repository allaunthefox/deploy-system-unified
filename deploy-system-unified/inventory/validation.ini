# Validation Inventory
# Used for clean syntax checks and local development testing.
# Does not represent a real deployment target.

[all_servers]
localhost ansible_connection=local

[production:children]
all_servers

[development:children]
all_servers

[ephemeral:children]
all_servers

[all_servers:vars]
virt_type=virtual
