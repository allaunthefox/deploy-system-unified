---
# Secret Provider Configuration
# Supported modes:
#   - vault: Only Ansible Vault secrets are enforced.
#   - sops: Only SOPS secrets are enforced.
#   - dual: Both providers must be valid and synchronized (Migration state).
secrets_provider_mode: "dual"
