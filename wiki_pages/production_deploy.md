# production_deploy

**Placeholder page.** This is the main production deployment playbook for the repo.

Canonical location:

- `projects/deploy-system-unified/production_deploy.yml`

Use `ansible-playbook production_deploy.yml -i inventory/<your_inventory>.ini` from the repo root.
